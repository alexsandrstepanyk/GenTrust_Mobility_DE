/**
 * API-only mode without Telegram bots
 * This is the development API server for mobile and frontend apps
 */

import "./api/server"; // Start API Server only

console.log("🚀 GenTrust API Server Started (API-only mode, no bots)");
